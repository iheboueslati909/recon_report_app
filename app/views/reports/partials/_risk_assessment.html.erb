<div class="page-break">
  <% risks = @report_data[:risk_assessment] || {} %>
  <% high_risks = risks[:high_risks] || [] %>
  <% medium_risks = risks[:medium_risks] || [] %>
  <% info_risks = risks[:info_risks] || [] %>

  <% if high_risks.any? || medium_risks.any? || info_risks.any? %>
    <div class="report-section">
      <h3 class="heading-secondary text-center">Risk Assessment</h3>

      <p class="text-regular mb-5">
        The following risks were identified during the reconnaissance phase. They are categorized by severity and may require immediate remediation or strategic consideration.
      </p>

      <%= render 'reports/partials/risk_section', title: 'High Risks', risks: high_risks, color_class: 'risk-high' if high_risks.any? %>
      <%= render 'reports/partials/risk_section', title: 'Medium Risks', risks: medium_risks, color_class: 'risk-medium' if medium_risks.any? %>
      <%= render 'reports/partials/risk_section', title: 'Informational', risks: info_risks, color_class: 'risk-low' if info_risks.any? %>
    </div>
  <% end %>
</div>
