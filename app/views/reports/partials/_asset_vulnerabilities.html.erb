<div>
  <h5 class="heading-sm mb-3">Top Vulnerabilities</h5>
  <% vulnerabilities.first(5).each do |vuln| %>
    <div class="mb-3">
      <div class="vuln-title mb-1"><%= vuln[:title] %></div>
      <div class="vuln-severity"><%= vuln[:severity] %> severity — CVSS: <%= vuln[:cvss] %></div>
      <div class="text-base"><%= vuln[:description] %></div>
    </div>
  <% end %>

  <% if vulnerabilities.size > 5 %>
    <p class="text-small text-muted"><em>Only the first 5 vulnerabilities are shown. See dashboard for more.</em></p>
  <% end %>
</div>
