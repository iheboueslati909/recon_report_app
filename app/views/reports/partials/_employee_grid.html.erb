<div class="flex flex-wrap gap-4">
  <% shown.each do |emp| %>
    <div class="employee-card">
      <p class="text-white mb-1"><%= emp[:fullname] %></p>
      <p class="text-sm text-muted"><%= emp[:poste].presence || "N/A" %></p>
    </div>
  <% end %>
</div>
