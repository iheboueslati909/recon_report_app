<div class="flex flex-wrap gap-4">
  <% shown.each do |emp| %>
    <div class="employee-card">
      <p class="text-white mb-0 font-bold"><%= emp[:fullname] %></p>
      <p class="text-muted mt-1"><%= emp[:poste].presence || "N/A" %></p>
    </div>
  <% end %>
</div>
